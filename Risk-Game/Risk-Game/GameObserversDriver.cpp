#include "GameEngine.h"

/*int main() {
	GameEngine game = GameEngine(true);

	//DisplayInfo one(1);
	//game.attachObserver(&one);
	//player->attachObserver(&one);

	game.mainGameLoop();
	return 0;
}*/